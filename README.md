# Forrum and v9ku bots

# Fix node-gyp For MacOS

```
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

```
brew install pkg-config cairo pango libpng jpeg giflib librsvg
```
